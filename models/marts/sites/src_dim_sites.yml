version: 2

sources:
  - name: stg_sites
    database: dev
    schema: incidents_stg
    tables:
      - name: stg_sites
        description: sites delta table
      - name: stg_dim_sites_030
        description: dim_sites penultimate staging table
      - name: stg_sites_extended_attributes
        description: sites_extended_attributes delta table